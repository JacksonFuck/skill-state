{
  "//": "Cole estas duas chaves dentro do objeto 'hooks' do settings.json (crie o objeto se não existir; preserve hooks já configurados). Padrão = install --global: ~/.claude/settings.json com os comandos abaixo. Estado fora de .skill-state/: acrescente '--dir <pasta>' ou exporte SKILL_STATE_DIR. O CLI não mora em <repo>/.claude/skills/.",
  "SessionStart": [
    {
      "matcher": "startup|resume|compact",
      "hooks": [
        {
          "type": "command",
          "command": "node \"$HOME/.claude/skills/skill-state/bin/cli.mjs\" context"
        }
      ]
    }
  ],
  "PreCompact": [
    {
      "matcher": "auto|manual",
      "hooks": [
        {
          "type": "command",
          "command": "node \"$HOME/.claude/skills/skill-state/bin/cli.mjs\" flush-check"
        }
      ]
    }
  ]
}
